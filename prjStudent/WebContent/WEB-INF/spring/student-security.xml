<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="auth/login.do" access="permitAll" />
		<intercept-url pattern="/logout.do" access="permitAll" />
		<intercept-url pattern="/batch/**" access="hasAnyRole('ROLE_ADMIN')" />
		<intercept-url pattern="/faculty/**" access="hasAnyRole('ROLE_ADMIN')" />
		<!-- <intercept-url pattern="/report/**" access="hasAnyRole('ROLE_REPORT','ROLE_ADMIN')" />		
		<intercept-url pattern="/**" access="hasAnyRole('ROLE_CARD','ROLE_MERCHANT','ROLE_ADMIN','ROLE_REPORT')" /> -->
		<form-login login-page="/auth/login.do" default-target-url="/auth/home.do" authentication-failure-url="/auth/login.do"  authentication-success-handler-ref="authSuccessHandler" />
		<logout logout-success-url="/auth/login.do" />
	</http>

	<authentication-manager alias="authenticationManager">
		<authentication-provider>
			<!-- <user-service>
				<user name="admin" password="admin" authorities="ROLE_ADMIN" />
			</user-service> -->
			<jdbc-user-service data-source-ref="dataSource" users-by-username-query="select username,password, enabled from users where username=?"
				authorities-by-username-query="select u.username,ur.authority from users u, user_roles ur where u.user_id = ur.user_id and u.username=?"
			/>		
		</authentication-provider>
	</authentication-manager>

</beans:beans>
    
    
    